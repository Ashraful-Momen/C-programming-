#include <stdio.h>
#include <string.h>
#include <conio.h>
#include <stdlib.h>
//Find The string of Length of String; by Build in Function:
int main ()
{
    char name[]="Shuvo";
    int length =strlen(name);
    printf("The Length of string is : %d",length);
    getch();
    return 0;
}
